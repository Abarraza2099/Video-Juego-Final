using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SetManager : MonoBehaviour
{
  //  string path = Application.streamingAssetsPath + "/Mario,jason"


//print(RenderingPath);

  //  CharacterData mario = new CharacterData();

    //    mario.name ="Mario";
      //  mario.weight = 98;
  //      mario.speed = 1.176f;
   //     mario.strength = 50;
  //      mario.frameData = 1.5f;
    //    mario.jumpDistance = 20:
  //      mario.damage = 0;

    // string json = JsonUtility.ToJson(mario, true);

   // Print(json);

   // File.WriteAllText(path, json);


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
